# Requirements Document

## Project Description (Input)
ヘルプデスク側のお知らせ管理機能。日本側ヘルプデスク担当者が、海外販社向けのお知らせを作成・編集・削除できる画面を、既存の`helpdesk-portal-layout`spec（`.kiro/specs/helpdesk-portal-layout/`）が確立したヘルプデスク側ルートセグメント・レイアウトの上に実装する。

背景: 既存の`announcements`spec（`.kiro/specs/announcements/`）は申請者側（海外販社担当者）向けのお知らせ一覧・詳細の閲覧機能のみを対象としており、「ヘルプデスク担当者向けのお知らせ作成・編集・削除機能」は明示的に対象外とされている。本specはこの対象外とされていた機能を実装する。壁打ちにより、配信対象を国・地域単位で指定できるようにすることが既に決まっている。

要件:
- ヘルプデスク側にお知らせ管理画面（一覧・新規作成・編集・削除）を新設する
- お知らせの項目はタイトル・本文・種別（既存の`maintenance`/`policy`/`incident`/`other`を流用）・公開日に加え、配信対象を国・地域単位で指定できるようにする（`Announcement`型の拡張が必要）
- 配信対象の指定は「全体一律」または「特定の国・地域のみ」を選べるようにする
- 配信対象の指定を実際に機能させるため、既存の申請者側のお知らせ一覧・詳細・ダッシュボードウィジェット（`announcements`・`dashboard`spec所有）が、配信対象に自社の国が含まれるお知らせ（または全体一律のお知らせ）のみを表示するようフィルタする対応を含む。フィルタの基準となる「自社の国」は、`helpdesk-inquiry-management`specで導入した自社スコープの考え方（固定のモック会社・国）を踏襲する
- ヘルプデスク側の変更系操作は、`helpdesk-inquiry-management`specで確立したパターン（Server Actions + `globalThis`ベースの共有モックストア + サーバー側バリデーション）を踏襲する
- `HelpdeskSidebar`にお知らせ管理へのナビゲーション項目を追加する
- 既存の申請者側のお知らせ一覧・詳細画面のレイアウト・操作性自体は変更しない（表示される件数がフィルタにより変わる点を除く）

スコープ外:
- 認証・ロールベースアクセス制御（フェーズ3以降）
- お知らせの既読・未読管理
- メール・プッシュ通知等の配信機能
- ヘルプデスク問い合わせ管理・テンプレート管理機能自体の変更（別spec `helpdesk-inquiry-management` で実装済み）

## Introduction

本仕様は、`helpdesk-portal-layout`specが確立したヘルプデスク側のルーティング・レイアウトの上に、既存の`announcements`specが対象外としていたヘルプデスク側のお知らせ作成・編集・削除機能を実装する。あわせて、お知らせごとに配信対象を国・地域単位で指定できるようにし、申請者側の閲覧画面がその配信対象に応じてフィルタされるようにすることで、機能全体を意味のある形で完結させる。

## Boundary Context

- **In scope**: ヘルプデスク側お知らせ管理画面（一覧・新規作成・編集・削除）、`Announcement`型への配信対象フィールドの追加、申請者側の読み取り関数（`getAnnouncements`・`getRecentAnnouncements`・`getAnnouncementById`）への配信対象フィルタの適用、`HelpdeskSidebar`へのナビゲーション項目追加
- **Out of scope**: 認証・ロールベースアクセス制御、お知らせの既読・未読管理、メール・プッシュ通知等の配信機能、`helpdesk-inquiry-management`（問い合わせ管理・テンプレート管理）機能自体の変更、`helpdesk-portal-layout`が確立したルートセグメント・レイアウト構造自体の変更
- **Adjacent expectations**: 既存の`announcements`spec・`dashboard`spec所有のコンポーネント（`AnnouncementList`・`AnnouncementDetail`・`AnnouncementWidget`）は、参照するデータの範囲（配信対象によるフィルタ）が変わるが、レイアウト・見た目・操作性は変更しない。ヘルプデスク側の変更系操作は、`helpdesk-inquiry-management`specが確立したパターン（Server Actions・共有モックストア・サーバー側バリデーション）を踏襲する前提とする

## Requirements

### Requirement 1: ヘルプデスク側お知らせ一覧の表示
**Objective:** As a ヘルプデスク担当者, I want 登録済みのお知らせを一覧で確認できる, so that 配信状況を把握し、編集・削除の対象を選べる

#### Acceptance Criteria
1. The Portal shall ヘルプデスク側にお知らせ管理画面を提供し、登録済みの全てのお知らせを公開日の降順で一覧表示する。
2. The Portal shall 一覧の各項目にタイトル・種別・公開日・配信対象（全体一律または対象国名）を表示する。
3. While データを読み込み中のとき、the Portal shall 一覧領域にローディング状態を表示する。
4. If データの取得に失敗したとき、the Portal shall エラーメッセージを表示する。
5. If お知らせが1件も存在しないとき、the Portal shall 「お知らせはありません」旨のメッセージを表示する。
6. The Portal shall 一覧から新規作成画面・各お知らせの編集画面への導線を提供する。

### Requirement 2: お知らせの新規作成
**Objective:** As a ヘルプデスク担当者, I want 新しいお知らせを作成できる, so that 海外販社へ周知事項を伝えられる

#### Acceptance Criteria
1. The Portal shall お知らせ新規作成画面に、タイトル・本文・種別・配信対象を入力するフォームを提供する。
2. If タイトル・本文・種別のいずれかが未入力のとき、the Portal shall 保存操作をブロックし入力を促す。
3. When ユーザーが必要項目を入力して保存したとき、the Portal shall 新しいお知らせを登録し、お知らせ管理一覧に反映する。
4. The Portal shall 新規作成時の公開日を保存操作を行った日時とする。

### Requirement 3: お知らせの編集
**Objective:** As a ヘルプデスク担当者, I want 既存のお知らせの内容を編集できる, so that 誤りの訂正や内容の更新ができる

#### Acceptance Criteria
1. The Portal shall 既存のお知らせを選択すると、現在の内容が初期表示された編集フォームを表示する。
2. If タイトル・本文・種別のいずれかが未入力のまま保存しようとしたとき、the Portal shall 保存操作をブロックし入力を促す。
3. When ユーザーが編集内容を保存したとき、the Portal shall 変更内容をお知らせ管理一覧・申請者側の表示に反映する。
4. If URLに存在しないお知らせIDが指定されたとき、the Portal shall お知らせが見つからない旨のメッセージを表示する。

### Requirement 4: お知らせの削除
**Objective:** As a ヘルプデスク担当者, I want 不要になったお知らせを削除できる, so that 一覧を最新の状態に保てる

#### Acceptance Criteria
1. The Portal shall お知らせ管理一覧または編集画面から、お知らせを削除する操作を提供する。
2. When ユーザーが削除操作を確定したとき、the Portal shall 対象のお知らせを削除し、お知らせ管理一覧・申請者側の表示から除去する。
3. The Portal shall 削除操作の実行前に、誤操作を防ぐための確認を求める。

### Requirement 5: 配信対象（国・地域）の指定
**Objective:** As a ヘルプデスク担当者, I want お知らせごとに配信対象を国・地域単位で指定できる, so that 関係する販社にのみ的確に情報を届けられる

#### Acceptance Criteria
1. The Portal shall お知らせの作成・編集フォームに、配信対象を「全体一律」または「特定の国・地域を1つ以上指定」のいずれかから選択できる操作を提供する。
2. If 「特定の国・地域を指定」を選択したとき、the Portal shall 既存の国コード一覧（`inquiryForm`の国選択肢）から1つ以上の国を選択できるようにする。
3. If 「特定の国・地域を指定」を選択したにもかかわらず1件も国が選択されていない状態で保存しようとしたとき、the Portal shall 保存操作をブロックし入力を促す。
4. The Portal shall 配信対象の指定内容をお知らせのデータとして保存する。

### Requirement 6: 申請者側での配信対象フィルタの適用
**Objective:** As a 海外販社担当者, I want 自社に関係するお知らせのみを確認したい, so that 関係のない地域向けの情報に惑わされない

#### Acceptance Criteria
1. The Portal shall 申請者側のお知らせ一覧・ダッシュボードウィジェットに、配信対象が「全体一律」のお知らせと、配信対象に自社の国が含まれるお知らせのみを表示する。
2. The Portal shall 申請者側のお知らせ詳細画面について、配信対象外のお知らせIDが指定されたとき、お知らせが見つからない旨のメッセージを表示する。
3. The Portal shall 本要件によるフィルタ適用後も、既存の申請者側画面のレイアウト・操作性（並び順・空状態・エラー表示等）を変更しない。

### Requirement 7: ヘルプデスク側ナビゲーションへの統合
**Objective:** As a ヘルプデスク担当者, I want サイドバーからお知らせ管理へ直接アクセスできる, so that 目的の画面にすぐ到達できる

#### Acceptance Criteria
1. The Portal shall `HelpdeskSidebar`のナビゲーション項目に、お知らせ管理画面への項目を追加する。
2. The Portal shall 追加した項目について、現在表示中のページに対応する項目をアクティブ状態で強調表示する。

### Requirement 8: 多言語対応（i18n）
**Objective:** As a ヘルプデスク担当者, I want お知らせ管理画面を日本語・英語で利用できる, so that 既存のポータルと同様に言語を切り替えて利用できる

#### Acceptance Criteria
1. The Portal shall 本specで追加する全ての画面・UI文字列を`next-intl`の翻訳キー経由で提供し、`messages/ja.json`・`messages/en.json`で管理する。
2. When 選択された言語の翻訳キーが存在しないとき、the Portal shall 既存と同様に英語（`en`）にフォールバックして表示する。

### Requirement 9: レスポンシブ対応
**Objective:** As a ヘルプデスク担当者, I want タブレット幅の端末からもお知らせ管理画面を問題なく利用できる, so that PC以外の環境でも業務を継続できる

#### Acceptance Criteria
1. The Portal shall 本specで追加する画面をタブレット幅（768px以上）のビューポートで横スクロールを発生させることなく表示する。

---

### 追加要望（2026-07-07）: タイトル・種別・対応要否による検索、対応要否フィールドの追加

壁打ちの結果、お知らせ管理一覧にも検索・絞り込み機能を追加すること、また各お知らせに「対応要否」（販社担当者が対応する必要があるかどうか）を設定できるようにすることが決まった。対応要否は`announcements`spec側で申請者側の一覧・詳細画面にバッジとして表示される（本specは設定・保持のみを担い、申請者側の表示ロジックは実装しない）。検索・絞り込みは既存の`helpdesk-inquiry-management`specが確立したフィルタパターン（フィルタ状態はクライアント側で保持し、選択の都度リアルタイムに一覧へ反映する）を踏襲する。

スコープ外:
- 対応要否バッジの申請者側での表示ロジック（`announcements`spec側で実装）
- お知らせの既読・未読管理（引き続き対象外）

### Requirement 10: 対応要否フィールドの追加と設定
**Objective:** As a ヘルプデスク担当者, I want お知らせごとに対応要否を設定できる, so that 販社担当者が対応の要不要をひと目で判断できるようにできる

#### Acceptance Criteria
1. The Portal shall `Announcement`型に対応要否を表す真偽値フィールド（`actionRequired`）を追加する。
2. The Portal shall お知らせの新規作成・編集フォームに、対応要否を選択する操作（トグルまたはチェックボックス）を提供する。
3. The Portal shall 新規作成時の対応要否の初期値を「対応不要」（`false`）とする。
4. The Portal shall お知らせ管理一覧の各項目に、対応要否が「対応が必要」の場合のみ、それと分かる表示（バッジ等）を行う。
5. When ユーザーが対応要否を変更して保存したとき、the Portal shall 変更内容をお知らせ管理一覧・申請者側の表示に反映する。

### Requirement 11: タイトル・種別・対応要否による検索・絞り込み
**Objective:** As a ヘルプデスク担当者, I want お知らせ管理一覧をタイトル・種別・対応要否で絞り込める, so that 登録件数が増えても目的のお知らせをすぐに見つけられる

#### Acceptance Criteria
1. The Portal shall お知らせ管理一覧に、タイトルのキーワードを入力する検索欄、種別を選択する絞り込み、対応要否を選択する絞り込みを提供する。
2. When ユーザーがキーワードを入力したとき、the Portal shall タイトルに入力文字列を含む（大文字・小文字を区別しない部分一致の）お知らせのみを一覧に表示する。
3. When ユーザーが種別または対応要否を選択したとき、the Portal shall 選択した条件に一致するお知らせのみを一覧に表示する。
4. When 複数の条件が指定されたとき、the Portal shall すべての条件を満たす（AND条件の）お知らせのみを一覧に表示する。
5. The Portal shall 絞り込みをページ全体の再読み込みなしに一覧へ即時反映する（既存の`helpdesk-inquiry-management`specのフィルタパターンを踏襲する）。
6. The Portal shall 絞り込み条件をクリアする操作を提供する。
7. If 絞り込み条件に一致するお知らせが1件も存在しないとき、the Portal shall 「該当するお知らせがありません」旨のメッセージを表示する。
8. The Portal shall 絞り込み適用後も、お知らせを公開日の降順で表示する順序を維持する。

---

### 追加要望（2026-07-08）: 確認済み・実施済み人数の可視化と未対応者へのリマインド

現状、お知らせが実際に海外販社担当者に閲覧・対応されたかを計測する手段がなく、アカウント機能が未実装のフェーズ1では実測はできない。壁打ちの結果、モックとして「担当者」データを新設し、お知らせごとの確認済み・実施済み人数の表示、未対応者一覧の確認、未対応者へのリマインド送信（モック）をヘルプデスク側に実装することが決まった。集計は個人（担当者）単位で行う。実施済みの集計は対応要否（`actionRequired`）が真のお知らせにのみ適用し、対応不要のお知らせでは確認済みのみを表示する。リマインドは実際の通知配信を行わず、送信完了の表示のみとする。海外販社側では本機能による確認済み・実施済みの人数集計・未対応者一覧は表示しない（`announcements`spec側には、別途リマインドが届いていることを示す表示のみを追加する）。

スコープ外:
- 実際のログイン・アカウント機能に基づくユーザー個人の識別（引き続き固定のモック担当者データを用いる）
- メール・プッシュ通知等、実際の通知配信
- 海外販社側での確認済み・実施済み人数や未対応者一覧の表示（`announcements`spec側では、リマインド受信の表示のみを対象とする）

### Requirement 12: 担当者マスタのモックデータ

**Objective:** As a ヘルプデスク担当者, I want お知らせの確認・対応状況を人単位で把握できる, so that 周知の浸透状況を正確に判断できる

#### Acceptance Criteria

1. The Portal shall 既存の会社一覧（`DOCUMENT_COMPANY_OPTIONS`）の各社について、複数名の担当者（氏名・所属会社・国）を持つモックデータを新設する。
2. The Portal shall 担当者データを、お知らせの確認済み・実施済み状況を追跡するための対象母集団として使用する。
3. Where お知らせの配信対象（`targeting`）が「全体一律」であるとき、the Portal shall 全担当者を集計対象とする。
4. Where お知らせの配信対象が「特定の国・地域」であるとき、the Portal shall 対象国に属する担当者のみを集計対象とする。

### Requirement 13: 確認済み・実施済み人数の表示

**Objective:** As a ヘルプデスク担当者, I want お知らせごとに確認済み・実施済みの人数を一目で把握できる, so that 対応漏れの兆候を早期に発見できる

#### Acceptance Criteria

1. The Portal shall お知らせ管理一覧の各項目に、「確認済み人数 / 対象人数」を表示する。
2. If お知らせの対応要否（`actionRequired`）が真であるとき、the Portal shall 「確認済み人数 / 対象人数」に加えて「実施済み人数 / 対象人数」を表示する。
3. If お知らせの対応要否が偽であるとき、the Portal shall 実施済み人数を表示しない。
4. The Portal shall 確認済み・実施済みの人数を、担当者ごとに保持するモックデータ（初期状態を含む）から算出する。
5. The Portal shall 確認済み・実施済みの人数表示のラベルを`next-intl`の翻訳キー経由で提供する。

### Requirement 14: 未対応者一覧とリマインド

**Objective:** As a ヘルプデスク担当者, I want 未確認・未対応の担当者を一覧で確認しリマインドを送信できる, so that 対応漏れを防ぎ周知を徹底できる

#### Acceptance Criteria

1. When ユーザーが確認済みまたは実施済みの人数表示をクリックしたとき、the Portal shall 未確認または未対応の担当者一覧（氏名・所属会社・国）をモーダルまたはドロワーで表示する。
2. The Portal shall 未対応者一覧の各担当者に対して、個別にリマインドを送信する操作を提供する。
3. The Portal shall 未対応者一覧に対して、一括でリマインドを送信する操作を提供する。
4. When ユーザーがリマインド送信操作を確定したとき、the Portal shall 実際の通知配信を行わず、対象担当者の「リマインド送信済み」状態をモックデータ上に記録し、送信完了を示すメッセージ（トースト等）を表示する。
5. If 担当者がすでにリマインド送信済みであるとき、the Portal shall 未対応者一覧に送信済みである旨を表示する。
6. The Portal shall 本要件で追加するUI文字列を`next-intl`の翻訳キー経由で提供する。

---

### 追加要望（2026-07-08）: 公開期間・対応期限の設定

現状、お知らせは公開日（`publishedAt`）のみを持ち、いつまで表示するかを制御する手段がない。また、対応要否（`actionRequired`）が「対応が必要」のお知らせについても、いつまでに対応すべきかを示す期限が存在しない。これにより、期限切れの周知が延々と表示され続けたり、販社担当者が対応の緊急度を判断できなかったりする課題がある。

これを解消するため、お知らせごとに次の2つの日時項目を追加する:

1. **公開期間**（開始日・終了日）: お知らせを海外販社側に表示する期間。公開期間が未設定の場合は、従来通り常時公開する。公開期間が設定されている場合、期間外（開始前・終了後）は海外販社側（`announcements`spec所有の一覧・詳細・ダッシュボードウィジェット）に表示しない。ヘルプデスク側の管理一覧では、公開期間に関わらず全件を表示する（期間外・公開予定分も確認・編集できる必要があるため）。
2. **対応期限**（`dueDate`）: 対応要否（`actionRequired`）が真の場合に対応を完了すべき期限。対応要否を「対応が必要」にした場合は入力必須とし、「対応不要」の場合は入力不可（未設定）とする。対応期限は`announcements`spec側の海外販社向け一覧・詳細画面にも表示する（別途`announcements`specへ要件追記済み）。

公開期間による表示可否の判定は、既存の配信対象（`targeting`）フィルタ（要件6）と同様の考え方で、申請者側の読み取り関数（`getAnnouncements`・`getRecentAnnouncements`・`getAnnouncementById`）に適用する。

スコープ外:
- 対応期限を過ぎたお知らせに対する自動的な状態変更やエスカレーション通知
- 公開期間・対応期限の変更履歴の記録

### Requirement 15: 公開期間の設定

**Objective:** As a ヘルプデスク担当者, I want お知らせごとに公開期間（開始日・終了日）を設定できる, so that 期限切れの周知が販社側に表示され続けることを防げる

#### Acceptance Criteria

1. The Portal shall お知らせの新規作成・編集フォームに、公開期間の開始日・終了日を入力する操作を提供する。
2. The Portal shall 公開期間の開始日・終了日をいずれも任意入力とする。
3. If 開始日・終了日の両方が入力され、終了日が開始日より前であるとき、the Portal shall 保存操作をブロックし入力を促す。
4. The Portal shall `Announcement`型に公開期間を表す任意項目（開始日・終了日）を追加する。
5. Where 公開期間が設定されていないお知らせであるとき、the Portal shall 当該お知らせを常時公開の状態として扱う。

### Requirement 16: 公開期間による表示制御

**Objective:** As a 海外販社担当者, I want 現在有効な期間のお知らせのみを確認したい, so that 終了済み・開始前のお知らせに惑わされない

#### Acceptance Criteria

1. Where お知らせに公開期間が設定されており、かつ現在日時が公開期間外（開始前または終了後）であるとき、the Portal shall 申請者側のお知らせ一覧・詳細・ダッシュボードウィジェットに当該お知らせを表示しない。
2. Where お知らせに公開期間が設定されていない、または現在日時が公開期間内であるとき、the Portal shall 申請者側の表示対象に当該お知らせを含める（要件6の配信対象フィルタと合わせて判定する）。
3. The Portal shall ヘルプデスク側のお知らせ管理一覧について、公開期間の状態（期間外・公開予定を含む）に関わらず全件を表示する。
4. The Portal shall お知らせ管理一覧の各項目に、公開期間が設定されている場合はその期間を、設定されていない場合は常時公開である旨を表示する。

### Requirement 17: 対応期限の設定

**Objective:** As a ヘルプデスク担当者, I want 対応が必要なお知らせに対応期限を設定できる, so that 販社担当者にいつまでに対応すべきかを明確に伝えられる

#### Acceptance Criteria

1. The Portal shall `Announcement`型に対応期限を表す任意項目（`dueDate`）を追加する。
2. If お知らせの新規作成・編集フォームで対応要否が「対応が必要」であるとき、the Portal shall 対応期限の入力欄を表示し入力を必須とする。
3. If 対応要否が「対応が必要」であるにもかかわらず対応期限が未入力のまま保存しようとしたとき、the Portal shall 保存操作をブロックし入力を促す。
4. If お知らせの新規作成・編集フォームで対応要否が「対応不要」であるとき、the Portal shall 対応期限の入力欄を無効化または非表示にし、対応期限を保持しない。
5. When ユーザーが対応要否を「対応が必要」から「対応不要」に変更したとき、the Portal shall 既に入力されていた対応期限をクリアする。
6. The Portal shall お知らせ管理一覧の各項目について、対応要否が真のお知らせにのみ対応期限を表示する。

---

### 追加要望（2026-07-10）: 下書き機能の追加

現状、お知らせは新規作成した瞬間から常に公開状態として扱われ、内容を検討中の段階や公開日をまだ確定していない段階のお知らせも、保存した時点で海外販社側に表示されてしまう。壁打ちの結果、お知らせに「下書き」と「公開」の2状態からなる公開状態を追加し、担当者が内容を練っている間は海外販社側に一切表示されないようにすることが決まった。

公開状態の切り替えは、新規作成・編集フォーム内の公開状態セレクト（下書き/公開）を選んで保存するだけで行う（保存ボタンは既存の1つのままとし、下書き保存・公開用の別ボタンは設けない）。この操作方法により、公開済みのお知らせを再び下書きへ差し戻す操作も同じセレクトで行える。

公開状態と、既存の公開期間（要件15・16）は独立した条件として扱う。公開状態が「公開」であることを満たした上で、さらに公開期間の条件（要件16）を満たす場合にのみ海外販社側に表示する。下書き中に公開期間を設定していても、公開状態を「公開」にしない限り一切表示されない。

スコープ外:
- 公開日時を指定した予約公開（自動的に下書きから公開へ切り替える機能）
- 公開状態の変更履歴の記録
- 下書き段階でのプレビュー専用画面（編集フォームでの確認を代用とする）

### Requirement 18: 公開状態（下書き/公開）フィールドの追加

**Objective:** As a ヘルプデスク担当者, I want お知らせごとに公開状態（下書き/公開）を設定できる, so that 内容を検討中の段階で誤って海外販社側に公開してしまうことを防げる

#### Acceptance Criteria

1. The Portal shall `Announcement`型に公開状態を表すフィールド（`status`、値は`draft`または`published`）を追加する。
2. The Portal shall お知らせの新規作成・編集フォームに、公開状態を「下書き」または「公開」から選択するセレクトを提供する。
3. The Portal shall 新規作成時の公開状態の初期値を「下書き」とする。
4. When ユーザーが公開状態を選択して保存したとき、the Portal shall 選択した公開状態でお知らせを保存する。
5. The Portal shall 公開状態が「公開」から「下書き」へ変更された場合も、同じ保存操作で受け付ける（下書きへの差し戻しに専用の操作を必要としない）。

### Requirement 19: 公開日時の記録タイミング

**Objective:** As a ヘルプデスク担当者, I want お知らせが実際に公開された日時が正しく記録される, so that 海外販社側での公開日表示や並び順が実態と一致する

#### Acceptance Criteria

1. When お知らせを公開状態「公開」で新規作成したとき、the Portal shall 保存操作を行った日時を公開日時として記録する。
2. When お知らせを公開状態「下書き」で新規作成したとき、the Portal shall 公開日時を未設定のまま保存する。
3. When 編集保存によって公開状態が「下書き」から「公開」に変わったとき、the Portal shall 保存操作を行った日時を公開日時として記録する。
4. If 公開状態が変更前後で「公開」のまま保存されたとき、the Portal shall 既存の公開日時を変更しない。
5. If 公開状態が「公開」から「下書き」に変更されたとき、the Portal shall 既存の公開日時を保持する（消去しない）。

### Requirement 20: 下書きの可視性制御

**Objective:** As a 海外販社担当者, I want 下書き状態のお知らせを一切見たくない, so that まだ確定していない情報に惑わされない

#### Acceptance Criteria

1. The Portal shall 申請者側のお知らせ一覧・ダッシュボードウィジェットについて、公開状態が「公開」であるお知らせのみを表示対象とする。
2. The Portal shall 申請者側のお知らせ詳細画面について、公開状態が「下書き」のお知らせIDが指定されたとき、お知らせが見つからない旨のメッセージを表示する。
3. The Portal shall 公開状態による本フィルタを、既存の配信対象フィルタ（要件6）・公開期間フィルタ（要件16）と組み合わせて（すべての条件を満たす場合のみ表示する形で）適用する。
4. The Portal shall ヘルプデスク側のお知らせ管理一覧について、公開状態に関わらず（下書き・公開のいずれも）全件を表示する。

### Requirement 21: 管理一覧での公開状態の表示・絞り込み

**Objective:** As a ヘルプデスク担当者, I want お知らせ管理一覧で下書きと公開済みを見分け、絞り込める, so that 公開作業の抜け漏れを防げる

#### Acceptance Criteria

1. The Portal shall お知らせ管理一覧の各項目について、公開状態が「下書き」のお知らせにそれと分かる表示（バッジ等）を行う。
2. The Portal shall お知らせ管理一覧に、公開状態（すべて/下書き/公開）で絞り込む操作を提供する（既存の要件11の絞り込みパターンを踏襲する）。
3. If お知らせの公開日時が未設定（下書きで公開日時が記録されていない）であるとき、the Portal shall 一覧の公開日表示についてエラーとならない代替表示を行う。

### Requirement 22: 下書き状態でのリマインド送信の抑止

**Objective:** As a ヘルプデスク担当者, I want 下書き状態のお知らせに対して誤ってリマインドを送信できないようにしたい, so that 未公開の情報について実在しない「未対応者」への通知を防げる

#### Acceptance Criteria

1. The Portal shall 公開状態が「下書き」のお知らせについて、確認済み・実施済み人数表示および未対応者一覧・リマインド送信操作（要件13・14）を提供しない、または操作不可の状態で表示する。

---

### 追加要望（2026-07-13）: 会社単位の自己申告記録機能（`announcements`spec連携）

`announcements`specへの要件追記により、海外販社担当者がお知らせ詳細画面を開くと自動的に「確認済み」が記録され、`actionRequired`が真のお知らせでは「対応完了にする」ボタン操作により「実施済み」が記録されるようになる。本specはこの自動記録・ボタン操作の両方を受け付け、既存の`AnnouncementRecipientStatus`（担当者単位の確認済み・実施済み状態）を会社単位で一括更新するサーバー側の記録関数を提供する。個人単位の識別は行わず、操作・閲覧した会社（`companyCode`）に属する担当者全員の状態を一括更新する。既存の確認済み・実施済み人数表示（要件13）・未対応者一覧・リマインド機能（要件14）は変更せず、本要件による記録もそれらの集計にそのまま反映される。

下書き状態（要件22）のお知らせについては、既存の抑止方針を踏襲し、本記録操作も受け付けない。

スコープ外:
- ヘルプデスク側UIの変更（本要件は`announcements`spec側から呼び出されるサーバー関数の追加のみを対象とする）
- 個人単位の識別・記録（申請者側には`backend-db-foundation`spec統合済みのログイン機能が既に存在するが、確認済み・実施済みの追跡対象である`AnnouncementRecipient`マスタはログインユーザー〔`ApplicantUser`〕とは紐づいていない別のモックデータであるため、本ラウンドでは既存の追跡モデルとの整合を優先し会社単位のまま扱う。個人単位への拡張は将来の検討事項とする）
- 記録の取り消し・変更履歴

### Requirement 23: 会社単位の確認済み・実施済み記録関数

**Objective:** As a 販社担当者（`announcements`spec側の詳細画面表示・ボタン操作を経由して）, I want 自社単位で確認済み・対応完了を記録できる, so that ヘルプデスク側の集計に実際の対応状況が反映される

#### Acceptance Criteria

1. The Portal shall 指定された`announcementId`・`companyCode`に基づき、当該会社に属する全ての`AnnouncementRecipient`の`confirmedAt`を記録するサーバー関数を提供する。
2. The Portal shall 同様に、当該会社に属する全ての`AnnouncementRecipient`の`completedAt`を記録するサーバー関数を提供する。
3. If 指定されたお知らせの公開状態が「下書き」であるとき、the Portal shall 本記録操作を受け付けない。
4. If 指定された`companyCode`が当該お知らせの配信対象に含まれないとき、the Portal shall 本記録操作を受け付けない。
5. The Portal shall 本要件による記録が、既存の確認済み・実施済み人数表示（要件13）・未対応者一覧（要件14）にそのまま反映されるようにする（別の集計経路を持たない）。
6. The Portal shall 既に`confirmedAt`（または`completedAt`）が記録済みの担当者について、再度の記録操作で上書きしない（初回記録時刻を保持する）。

---

### 追加要望（2026-07-15）: 添付ファイル（自身のファイル共有・ドキュメント管理からの紐づけ）

現状、お知らせにファイルを添付する手段がない。壁打ちの結果、お知らせの新規作成・編集フォームに、次の2種類の添付方法を追加することが決まった。

1. **直接アップロード（自身のファイル共有）**: `inquiry-form`spec所有の問い合わせ添付機能と同じ仕組み（`FileReader.readAsDataURL`によるBase64データURL方式、許可形式・サイズ制限）でファイルをその場でアップロードする。
2. **ドキュメント管理からの紐づけ**: `documents-management`spec配下に既に登録済みの`Document`を、コピーではなく参照（ID）として選択・紐づける。

添付ファイルは、既存のお知らせ一覧・ダッシュボードウィジェットのレイアウト・操作性を変更しないため、海外販社側の**詳細画面にのみ**表示する。ドキュメント紐づけについては、紐づけ先`Document`自身の公開範囲（`DocumentTargeting`）を優先し、お知らせの配信対象とは独立に、閲覧者の会社・国が紐づけ先ドキュメントの公開範囲に含まれない場合は当該添付を表示しない（不一致の警告等は行わない）。詳細画面での添付ドキュメントの見え方は、既存の申請者側ドキュメント一覧画面（`documents`spec所有）と同様に、`PdfViewer`によるインラインプレビュー（iframe＋ダウンロードリンク）とする。

スコープ外:
- `Document`自体の新規登録・編集・削除・公開範囲設定（`documents-management`spec所有、本specはIDによる参照のみ）
- お知らせの配信対象とドキュメントの公開範囲が不一致の場合の警告・保存時のブロック
- 添付ファイルの一覧画面・ダッシュボードウィジェットへの表示（詳細画面のみを対象とする）
- 実ファイルストレージへの保存（フェーズ1はBase64データURL方式を継続）

### Requirement 24: 添付ファイル（自身のファイル共有・ドキュメント管理からの紐づけ）

**Objective:** As a ヘルプデスク担当者, I want お知らせに関連資料をファイルとして添付できる, so that 海外販社担当者に必要な資料を確実に届けられる

#### Acceptance Criteria

1. The Portal shall お知らせの新規作成・編集フォームに、ファイルを直接アップロードして添付する操作を提供する。
2. The Portal shall 直接アップロードする添付ファイルについて、`inquiry-form`spec所有の許可形式（画像・PDF）・サイズ上限（1件5MB）・件数上限（最大5件）の検証をそのまま適用し、違反時は保存操作をブロックし入力を促す。
3. The Portal shall アップロードされた添付ファイルの内容を、`FileReader.readAsDataURL`によるBase64データURL文字列として保持する。
4. The Portal shall お知らせの新規作成・編集フォームに、`documents-management`spec配下に登録済みの`Document`を一覧から選択して紐づける操作を提供する。
5. The Portal shall ドキュメント紐づけについて、絞り込みなしの全登録ドキュメントから最大5件まで選択できるようにする。
6. The Portal shall 新規作成・編集フォームで選択・アップロード済みの添付ファイル・紐づけドキュメントについて、個別に選択解除（削除）できる操作を提供する。
7. When ユーザーが添付ファイル・紐づけドキュメントを含めてお知らせを保存したとき、the Portal shall 保存内容をお知らせのデータとして永続化する。
8. The Portal shall 直接アップロードの添付ファイル・紐づけドキュメントの両方を、海外販社側のお知らせ**詳細画面にのみ**表示し、一覧画面・ダッシュボードウィジェットの表示内容・レイアウトは変更しない。
9. The Portal shall 詳細画面において、直接アップロードの添付ファイルを（画像はサムネイル付き、その他はファイル名・サイズの一覧として）常に表示する。
10. The Portal shall 詳細画面において、紐づけドキュメントのうち、閲覧者の会社・国が当該`Document`自身の公開範囲（`DocumentTargeting`）に含まれるものについてのみ、既存の申請者側ドキュメント一覧画面と同様の`PdfViewer`（インラインPDFプレビュー＋ダウンロードリンク）で表示する。
11. If 紐づけドキュメントの公開範囲に閲覧者の会社・国が含まれないとき、the Portal shall 当該添付を詳細画面に一切表示しない（リンク切れ等の表示は行わない）。
12. The Portal shall ドキュメント紐づけ時に、お知らせの配信対象とドキュメントの公開範囲との一致・不一致を検証・警告しない（要件10の可視性制御のみで整合を取る）。

---

### 追加要望（2026-07-15）: 添付ファイル・紐づけドキュメントのPDFインラインプレビュー

現状、要件24で追加した添付ファイル・紐づけドキュメントの表示は次の通りである: 海外販社側詳細画面では、紐づけドキュメントのみ`PdfViewer`によるインラインプレビューがあり、直接アップロードした添付ファイルはPDFであってもファイル名・サイズの一覧表示に留まる（要件24項目9）。また、ヘルプデスク側の新規作成・編集フォームには、直接アップロード・紐づけドキュメントのいずれについてもプレビューが一切なく、ファイル名の一覧表示のみである。

`documents-management`spec側の詳細/編集画面（`DocumentDetailPanel`）では、既に登録済みドキュメントの`PdfViewer`によるインラインプレビューが表示モード・編集モードの両方で提供されている。壁打ちの結果、これと同様のプレビュー体験を、お知らせの添付ファイル・紐づけドキュメントについても、ヘルプデスク側の新規作成・編集フォームと海外販社側詳細画面の両方に広げることが決まった。ダウンロード導線（`PdfViewer`のダウンロードリンク）は引き続き提供する。

対象はPDF形式の添付ファイル・紐づけドキュメントのみとする。画像添付（`image/*`）は既存のサムネイル表示を変更しない。なお紐づけドキュメントは`documents-management`spec側の制約（`DOCUMENT_ALLOWED_MIME_TYPES`がPDFのみ）により常にPDFであるため、紐づけドキュメントは全件がプレビュー対象となる。

スコープ外:
- 画像添付のプレビュー表示方法の変更（既存のサムネイル表示を維持する）
- 一覧画面・ダッシュボードウィジェットへのプレビュー表示の追加（要件24と同様、詳細画面・管理フォームのみを対象とする）
- 紐づけドキュメントの公開範囲（`DocumentTargeting`）による可視性制御ロジックの変更（要件24項目10・11を維持し、表示方法のみを変更する）
- PDF・画像以外のファイル形式への対応拡大（現状の許可形式は画像・PDFのみのため対象外）

### Requirement 25: 添付ファイル・紐づけドキュメントのPDFインラインプレビュー

**Objective:** As a ヘルプデスク担当者・海外販社担当者, I want お知らせに添付・紐づけられたPDFファイルをその場でプレビューできる, so that ファイルをダウンロードして別アプリで開かなくても内容を確認できる

#### Acceptance Criteria

1. The Portal shall お知らせの新規作成・編集フォーム（`AnnouncementForm`）において、直接アップロードされたPDF形式の添付ファイルを、`documents`spec所有の`PdfViewer`によるインラインプレビュー（iframe埋め込み＋ダウンロードリンク）で表示する。
2. The Portal shall お知らせの新規作成・編集フォームにおいて、紐づけ済みの`Document`（PDF形式）についても、同様に`PdfViewer`によるインラインプレビューで表示する。
3. The Portal shall 新規作成・編集フォームにおける画像形式の直接アップロード添付ファイルについて、要件24で定義した既存のサムネイル表示を変更しない。
4. The Portal shall 海外販社側のお知らせ詳細画面（`AnnouncementDetail`）において、直接アップロードされたPDF形式の添付ファイルを`PdfViewer`によるインラインプレビューで表示する（要件24項目9のうち、PDF形式の添付ファイルに関する表示方法をこの表示に置き換える）。
5. The Portal shall 海外販社側のお知らせ詳細画面における画像形式の直接アップロード添付ファイルについて、要件24項目9で定義した既存のサムネイル表示を変更しない。
6. The Portal shall 本要件によるプレビュー表示のいずれにおいても、`PdfViewer`が提供するダウンロードリンクを介してファイルをダウンロードできる状態を維持する。
7. The Portal shall 紐づけドキュメントのプレビュー表示について、要件24項目10・11で定義した公開範囲（`DocumentTargeting`）による可視性制御を変更しない（本要件は表示方法の変更のみを対象とする）。
8. The Portal shall フォーム・詳細画面のいずれにおいても、複数のPDF添付・紐づけドキュメントが存在する場合、それぞれを個別の`PdfViewer`インスタンスとして表示する。

---

### 追加要望（2026-07-16）: 通知配信と多言語発信対応

現状、お知らせの公開・リマインド送信はポータル内表示のみであり、海外販社担当者がログインしなければ一切気づけない。本ポータルの目的は「日本本社から海外販社・代理店（20か国以上）への情報発信」であり、受信者側の能動的なログインに依存しないプッシュ型の届け方が存在しないことは構造的な課題である。また、お知らせのタイトル・本文は単一言語（ヘルプデスク担当者が入力した言語のまま）でしか保持できず、UIロケール（`ja`/`en`）の切り替えに関わらず本文の言語は変わらない。20か国以上への発信を想定すると、日本語または英語のみの発信では正確に伝わらない受信者が生まれる可能性が高い。

この2点は「多言語コンテンツを、各受信者の言語で通知配信する」という一体の機能であるため、本ラウンドでまとめて要件化する。フェーズ1で「メール・プッシュ通知等の配信機能」を対象外としていた制約は、本追記により見直す。

技術方式（メール送信基盤の具体的な選定、`AnnouncementRecipient`と`ApplicantUser`の紐付け方法の詳細等）は設計フェーズで検討する。

スコープ外:
- SMS・アプリ内プッシュ通知等、メール以外の配信チャネル
- 受信者ごとの通知設定（配信オプトアウト等）のUI（将来検討事項とし、本ラウンドは全対象受信者への一律送信とする）
- ヘルプデスク側担当者マスタ（`AnnouncementRecipient`）へのログイン機能の付与、個人単位の識別への拡張（引き続き会社単位の扱いを維持する）
- 対応要否・対応期限・添付ファイル等、既存要件（要件1〜25）の変更
- 20か国語すべてを網羅する翻訳データの自動生成・機械翻訳連携

### Requirement 26: メール送信基盤の導入

**Objective:** As a ヘルプデスク担当者, I want お知らせの公開・リマインドが対象受信者に自動的にメールで届く, so that 受信者がポータルにログインしなくても重要な発信に気づける

#### Acceptance Criteria
1. The Portal shall メール送信基盤（SMTP/Nodemailerまたは同等の仕組み）を導入する。
2. The Portal shall メール送信基盤の接続設定（SMTPホスト・認証情報等）を環境変数経由で構成できるようにする。
3. The Portal shall メール送信処理を、既存のServer Actions/サービス層（`announcement-service.ts`等）から呼び出せる独立したモジュールとして提供する。

### Requirement 27: お知らせ公開時のメール通知送信

**Objective:** As a 海外販社担当者, I want 自社宛のお知らせが公開された際にメールで知らされたい, so that ポータルにログインしなくても新しい周知事項に気づける

#### Acceptance Criteria
1. When ヘルプデスク担当者がお知らせを公開状態（`published`）で保存したとき（新規作成、または下書きからの遷移）、the Portal shall 当該お知らせの配信対象（要件5の`targeting`）に含まれる対象受信者へ通知メールを送信する。
2. The Portal shall 通知メールの宛先を、配信対象に含まれる国に属する`ApplicantUser`のうち有効なメールアドレス（`email`）を持つ全員とする。
3. If 公開状態が「公開」のまま編集保存されたとき（`draft`→`published`の遷移を伴わない場合）、the Portal shall 通知メールを再送しない。
4. The Portal shall 通知メールの本文に、お知らせのタイトル・本文の要約またはポータルへの詳細リンクを含める。

### Requirement 28: リマインド送信時のメール通知送信

**Objective:** As a 海外販社担当者, I want 未対応のお知らせについてリマインドされた際にメールで知らされたい, so that 対応漏れに気づける

#### Acceptance Criteria
1. When ヘルプデスク担当者が未対応者一覧から個別または一括でリマインド送信操作を確定したとき、the Portal shall 対象受信者（未確認または未実施の担当者が属する会社の対象`ApplicantUser`）へリマインドメールを送信する。
2. The Portal shall リマインドメールの本文に、対象のお知らせのタイトル・対応期限（設定されている場合）・ポータルへの詳細リンクを含める。
3. The Portal shall 既存のリマインド送信操作（要件14）の完了表示・送信済み記録の挙動を変更しない（メール送信は既存操作に付随する追加処理として実装する）。

### Requirement 29: 送信失敗時のベストエフォート処理と送信履歴記録

**Objective:** As a ヘルプデスク担当者, I want メール送信に失敗しても公開・リマインド操作自体は成功させたい, so that メール基盤の不調が業務操作をブロックしない

#### Acceptance Criteria
1. If 通知メールの送信に失敗したとき、the Portal shall 公開操作・リマインド送信操作自体を失敗させず、正常に完了させる（ベストエフォート方式）。
2. When 通知メールの送信に失敗したとき、the Portal shall 失敗の内容（対象お知らせ・宛先・エラー内容）をログに記録する。
3. The Portal shall 通知メールの送信結果（成功・失敗、送信先件数、送信日時）の履歴を記録する。
4. The Portal shall 送信履歴について、少なくともヘルプデスク側で最低限の確認ができる手段（一覧表示への件数反映等、詳細UIは設計フェーズで検討する）を設ける。

### Requirement 30: 受信者（ApplicantUser）の言語設定

**Objective:** As a 海外販社担当者, I want 自分の言語設定に応じたメール・お知らせ内容を受け取りたい, so that 母国語または理解できる言語で情報を得られる

#### Acceptance Criteria
1. The Portal shall `ApplicantUser`に、通知メール・お知らせ表示に用いる言語設定を表す項目を追加する（データモデル変更を伴い、マイグレーションが必要になる）。
2. The Portal shall 言語設定が未設定の`ApplicantUser`について、既定言語（`en`）を用いたものとして扱う。
3. The Portal shall 通知メールの言語を、宛先`ApplicantUser`の言語設定に応じて決定する。

### Requirement 31: お知らせタイトル・本文の多言語コンテンツ化

**Objective:** As a ヘルプデスク担当者, I want お知らせのタイトル・本文を複数言語で登録できる, so that 海外の受信者に正確に内容を伝えられる

#### Acceptance Criteria
1. The Portal shall `Announcement`のタイトル・本文を、単一言語のフィールドから言語別に保持できる構造に変更する（データモデル変更を伴い、マイグレーションが必要になる）。
2. The Portal shall 既存UIロケール（`ja`・`en`）に対応するタイトル・本文の入力を必須とする。
3. The Portal shall `ja`・`en`以外の言語のタイトル・本文の入力を任意とする。
4. Where ある言語のタイトル・本文が入力されていないとき、the Portal shall 既定言語（`ja`）の内容にフォールバックして提供する。
5. The Portal shall 既存の`title`/`body`単一フィールドを参照している箇所（申請者側読み取り関数等）に対して、後方互換性を維持する移行方針（既存データの既定言語への割り当て等）を設計フェーズで定める。

### Requirement 32: ヘルプデスク側フォームでの言語別入力

**Objective:** As a ヘルプデスク担当者, I want お知らせ作成・編集フォームで言語ごとにタイトル・本文を入力できる, so that 一度の作成操作で必要な言語分のコンテンツをまとめて用意できる

#### Acceptance Criteria
1. The Portal shall お知らせの新規作成・編集フォームに、言語ごとにタイトル・本文を入力するUI（タブ切り替え等）を提供する。
2. If `ja`・`en`のいずれかのタイトルまたは本文が未入力のまま保存しようとしたとき、the Portal shall 保存操作をブロックし入力を促す。
3. The Portal shall 任意言語（`ja`・`en`以外）の追加・入力・削除を、既存の必須言語の入力を妨げない形で提供する。
4. The Portal shall 言語別入力に関するUI文字列を`next-intl`の翻訳キー経由で提供する。

### Requirement 33: 通知メールの言語別本文生成

**Objective:** As a 海外販社担当者, I want 自分の言語設定に対応するお知らせ内容でメールを受け取りたい, so that 内容を正確に理解できる

#### Acceptance Criteria
1. The Portal shall 公開時・リマインド時の通知メール（要件27・28）の本文を、宛先`ApplicantUser`の言語設定（要件30）に対応するお知らせのタイトル・本文（要件31）から生成する。
2. If 宛先の言語設定に対応するタイトル・本文が入力されていないとき、the Portal shall 既定言語（`ja`）の内容にフォールバックしてメール本文を生成する。
3. The Portal shall 同一お知らせの公開・リマインドについて、宛先の言語設定が異なる受信者に対し、それぞれ対応する言語のメール本文を送信する（受信者ごとに言語が異なってよい）。

### 追加要望（2026-07-21）: 配信対象国・地域選択UIの刷新

背景: プロダクト全体レビューにより、`AnnouncementForm`の配信対象「特定の国・地域を指定」で表示される国選択UIが、素のネイティブ`<select multiple>`のままであることが判明した。`INQUIRY_COUNTRY_CODES`（23か国）規模の選択肢に対し、Ctrl+クリックによる多重選択が必須で直感的でない、検索機能がない、全選択・選択解除の一括操作がない、現在選択されている国が一覧としてぱっと見て確認しにくい、という操作性上の課題がある。要件5（配信対象の指定）を対象に、UIコンポーネントを刷新することで解消する。

補足（今回のスコープ外とする事項）: 地域（東南アジア等）単位でのグルーピング表示は、そのような分類メタデータが現状コードベース（`INQUIRY_COUNTRY_CODES`）に存在せず、新設には别途要件定義が必要なため、本追加要望では対象外とする（将来要件として持ち越す）。同様に、配信対象の確定前に対象会社数・対象人数を集計して確認表示する機能（例:「対象X社に配信されます」）も、会社データとの追加連携が必要になるため、本追加要望のスコープには含めない（将来要件として持ち越す）。

### Requirement 34: 配信対象国・地域選択UIの刷新

**Objective:** As a ヘルプデスク担当者, I want 配信対象の国・地域を検索・一括操作・選択状態の可視化を伴うUIで選べる, so that 23か国規模の選択肢からでも直感的かつ正確に配信対象を指定できる

#### Acceptance Criteria
1. The Portal shall お知らせの作成・編集フォームで配信対象を「特定の国・地域を指定」にしたとき、ネイティブの`<select multiple>`に代えて、検索ボックス・チェックボックス形式の一覧・選択済み国の一覧表示を備えた国・地域選択UIを提供する。
2. The Portal shall 当該UIに、国名（表示ラベル）による絞り込み検索を提供する。検索語に一致する候補が0件のとき、その旨のメッセージを表示する。
3. The Portal shall 当該UIに、表示中の候補全件を一括選択する操作と、選択済みの国・地域を一括解除する操作を提供する。
4. The Portal shall 現在選択されている国・地域の一覧（チップ表示等）および選択件数を、常時確認できる形で表示する。チップから個別に選択を解除できる。
5. The Portal shall 当該UIについて、キーボード操作（Tab移動・チェックボックスの選択/解除）およびラベル付け（スクリーンリーダーでの読み上げ）に配慮したマークアップとする。
6. The Portal shall 当該UIの選択状態を、既存のreact-hook-form + zodバリデーション（要件5の1件以上選択必須の検証を含む）と統合し、送信データの形式（選択された国コードの配列、`targeting.countries`）を変更しない。
7. The Portal shall 当該UIの選択状態表現（選択済みチェックボックス・チップの配色等）に、ブランドガイドライン（`.kiro/steering/brand.md`）のDAISOピンク（`--primary`）をCSS変数経由で使用し、色をハードコードしない。
8. The Portal shall 当該UIの表示文字列を`next-intl`の翻訳キー経由で提供し、日本語・英語の両方に対応する。

---

### 追加要望（2026-07-22）: 未整備言語のフォールバック先を英語（共通語）優先に変更

2026-07-21実施のプロダクト全体レビューにより、多言語コンテンツの解決関数`resolveAnnouncementContent`（`src/lib/server/announcement-mapper.ts`）のフォールバック順序が、20か国以上へ発信する本ポータルの実態に合っていないことが判明した。現状は、受信者の`preferredLocale`（例: `th`・`vi`）に対応する翻訳が未登録のとき、**英語（`en`）ではなく日本語（`ja`）にフォールバック**している（要件31.4・33.2で「既定言語（`ja`）にフォールバック」と定義）。しかし`en`は必須入力（要件31.2）であり実質的に全お知らせに存在する一方、`ja`（既定言語カラム）は多くの海外受信者にとって最も理解しにくい言語である。海外販社・代理店（20か国以上）の共通語は英語であるため、未整備言語のフォールバック先は`ja`ではなく`en`を優先すべきである。

本追記は、`resolveAnnouncementContent`のフォールバック順序を **「`preferredLocale`一致 → `en` → `ja`」** に変更する。要件31.4・33.2で定めた「`ja`へフォールバック」の挙動を本追記の内容で上書きする（`ja`は`en`翻訳も存在しない場合の最終フォールバックとして残す）。

この変更の影響範囲は`resolveAnnouncementContent`を経由する全経路である。具体的には (a) 通知メール本文の生成（要件33、`announcement-notifications.ts`、`preferredLocale`が`th`/`vi`等の受信者で実際に挙動が変わる）と、(b) 申請者側のお知らせ表示（`announcements`spec要件16、UIロケールは`ja`/`en`のみのため実挙動は不変）である。(b)は`ja`/`en`しか渡らないため表示結果は変わらないが、関数の契約としては一貫して`en`優先フォールバックとする。

あわせて、通知メールの**詳細リンクのUIロケール**（`resolveUiLocale`、`announcement-notifications.ts`）も、現状は`routing.locales`に含まれない`preferredLocale`（`th`/`vi`等）を`routing.defaultLocale`（`ja`）にフォールバックしている。本文が英語で生成されるのにリンク先が日本語UIページになる不整合を避けるため、詳細リンクのUIロケールのフォールバック先も`en`に統一する。

スコープ外:
- 20か国語すべての翻訳データの整備・機械翻訳連携（引き続き対象外。あくまで未整備時のフォールバック先の変更のみ）
- `ApplicantUser.preferredLocale`の既定値（`en`）の変更（要件30.2を維持）
- 申請者側UIロケールの追加（`ja`/`en`以外のUIルーティングは引き続き対象外）

### Requirement 35: 多言語フォールバック順序の英語優先化

**Objective:** As a 海外販社担当者（英語圏外の受信者を含む）, I want 自分の言語の翻訳が未整備のとき、日本語ではなく英語で内容を受け取りたい, so that 共通語である英語で内容を理解できる

#### Acceptance Criteria

1. The Portal shall `resolveAnnouncementContent(announcement, locale)`のフォールバック順序を「`locale`一致 → `en` → `ja`（既定言語カラム）」とする。すなわち、`locale`に一致する翻訳が存在すればそれを、なければ`en`翻訳を、`en`翻訳も存在しなければ`ja`（既定言語カラムの`title`/`body`）を返す。
2. When `locale`が`ja`であるとき、the Portal shall 従来どおり既定言語カラム（`announcement.title`/`body`）を返す（`ja`は最優先で確定するため`en`を経由しない）。
3. When `locale`が`en`であり`en`翻訳が存在するとき、the Portal shall 当該`en`翻訳を返す（一致優先。要件31.2により`en`は必須のため通常はここで確定する）。
4. When 通知メール（要件33）の宛先`preferredLocale`が`en`・`ja`以外（`th`・`vi`等）であり、対応する翻訳が未登録であるとき、the Portal shall `en`翻訳の内容でメール本文（タイトル・本文）を生成する（`en`翻訳も無い場合のみ`ja`）。
5. The Portal shall 要件31.4・33.2で定めた「`ja`へフォールバック」の記述を、本要件の「`en`優先（`en`が無い場合のみ`ja`）」に読み替える（`resolveAnnouncementContent`の単一実装変更で両要件の挙動を同時に更新する）。
6. The Portal shall 通知メールの詳細リンクに用いるUIロケール（`resolveUiLocale`）について、`routing.locales`（`ja`/`en`）に含まれない`preferredLocale`のフォールバック先を、`routing.defaultLocale`（`ja`）ではなく`en`とする（本文言語とリンク先UIロケールの整合をとる）。
7. The Portal shall 本変更により影響を受ける既存の単体テスト（`resolveAnnouncementContent`の`th`フォールバック、通知メールの`preferredLocale: "th"`時の件名・本文・詳細リンク）の期待値を、`en`優先の新挙動に更新する。
